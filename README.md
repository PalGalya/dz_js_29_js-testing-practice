# Обробка подій JavaScript та Тестування - ДЗ #12

Навчальний проект з роботи з подіями DOM та написання модульних тестів.

## 🚀 Швидкий старт

### Встановлення залежностей

```bash
# Використання npm
npm install

# Використання yarn (потребує глобального встановлення)
npm i -g yarn && yarn
```

## 📝 Опис завдань

Проект містить три основні завдання з обробки подій DOM:

1. **Обробка кліка кнопки** - Функція `handleButtonClick()` для відстеження кліків по кнопці
2. **Відстеження позиції курсору** - Функція `trackMousePosition()` для логування координат миші
3. **Делегування подій** - Функція `setupEventDelegation()` для обробки кліків по елементах списку

Кожна функція має свій набір модульних тестів у директорії `src/__test__/`.

## Тестування

### Тестові випадки для handleButtonClick:

- Базовий клік по кнопці
- Різні повідомлення
- Неіснуючі елементи
- Множинні кліки
- Невалідні параметри
- Порожні значення

### Запуск тестів

```bash
npm test
```

### Покриття коду

```bash
npm test -- --coverage
```

### Вимоги до тестів

- ✓ Використання блоків `describe`
- ✓ Наявність тестових випадків `test` або `it`
- ✓ Успішне проходження всіх тестів

## ✅ Перевірка виконання

1. Переконайтеся що всі тести проходять успішно
2. Зробіть скріншот результатів тестування
3. Завантажте проект на GitHub
4. Надайте посилання на JavaScript файли в репозиторії

## 📁 Структура проекту

```
src/
  ├── main.js           # Основний код з реалізацією функцій
  ├── _index.html       # HTML для тестування
  └── __test__/         # Директорія з тестами
      ├── handleButtonClick.test.js
      ├── trackMousePosition.test.js
      └── setupEventDelegation.test.js
```

## 🛠️ Технології

- JavaScript (ES6+)
- Vitest - фреймворк для тестування
- JSDOM - емуляція браузерного середовища
- Testing Library - утиліти для тестування
